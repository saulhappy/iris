import React from "react";
import { Link } from "react-router-dom";
import PropTypes from "prop-types";

const ProfileItem = (props) => {
  return <div>should be a profile</div>;
};

ProfileItem.propTypes = {};

export default ProfileItem;
